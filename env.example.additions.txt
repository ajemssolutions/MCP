# --- AJEMS Connector Store status reporting ---
# Best effort: a failure here never affects OAuth, MCP requests, tools or syncing.
CONNECTOR_STORE_REPORT_URL=https://connectors.ajems.com/api/connectors/claude/report
STATUS_REPORTING=true          # set false to disable reporting entirely
STATUS_HEARTBEAT_MS=300000     # 5 minutes — keeps the Store showing Online
STATUS_REPORT_TIMEOUT_MS=10000 # give up on a slow Connector Store
STATUS_EVENT_THROTTLE_MS=60000 # at most one activity event per org per minute
